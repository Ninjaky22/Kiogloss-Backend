spring.application.name=store-api

# Server Configuration
server.port=8000
server.servlet.context-path=/API/V1.0

# Database Configuration (PostgreSQL)
spring.datasource.url=jdbc:postgresql://localhost:5432/store
spring.datasource.username=postgres
spring.datasource.password=1234
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate Configuration
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

# JWT Configuration (equivalent to Django's SIMPLE_JWT)
jwt.secret=django-insecure-r0n0n9tkdg7o@u%+80xp!fyt!5m_)yzsdv8flk_vr-=3sb=xew
jwt.access-token-expiration=300000
jwt.refresh-token-expiration=86400000

# Cloudinary Configuration
cloudinary.cloud-name=dqxq7tupx
cloudinary.api-key=766931669467673
cloudinary.api-secret=RgPWDVJJHRln7SfqQy0guySl_YA

# File Upload Configuration
spring.servlet.multipart.max-file-size=1GB
spring.servlet.multipart.max-request-size=1GB
app.upload.dir=uploads

# Tomcat Configuration
server.tomcat.max-swallow-size=-1

# JSON Configuration
spring.jackson.serialization.write-dates-as-timestamps=false
spring.jackson.time-zone=UTC

# CORS Configuration
cors.allowed-origins=*
cors.allowed-methods=GET,POST,PUT,DELETE,PATCH,OPTIONS
cors.allowed-headers=*
cors.allow-credentials=true

# Logging
logging.level.com.ecommerce=DEBUG
logging.level.org.hibernate.SQL=DEBUG

# Mail (Gmail)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=casanovacristian40@gmail.com
spring.mail.password=ldafiwvlqgtbjaox
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# URL del frontend (para el link del email)
app.frontend-url=http://localhost:5173